{
  "name": "sharepoint-accessor-get",
  "version": "0.0.0",
  "description": "",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build/src"
  ],
  "devDependencies": {
    "bun-types": "^0.2.0",
    "gts": "^3.1.1",
    "typescript": "~4.7.0",
    "@types/node": "^14.11.2"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "start": "functions-framework --source=build/src/ --target=helloWorld",
    "watch": "concurrently 'tsc -w' 'nodemon --watch ./build/ --exec bun run start'",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "bun run compile",
    "pretest": "bun run compile",
    "posttest": "bun run lint"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.1.3",
    "@types/express": "^4.17.15",
    "concurrently": "^7.6.0",
    "nodemon": "^2.0.20"
  }
}
